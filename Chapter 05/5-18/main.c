/**
 * Chapter: 5
 * Exercise: 5-16 - Add -d ("directory order") option, which makes comparisons only on letters, numbers and blanks. Make sure
 * it works in conjunction with -f.
 **/

#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}